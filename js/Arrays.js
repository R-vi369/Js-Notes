// array is a variable use to store multiple values at a time
  
// starts with 0 index
  const lang = ['js', 'py', 'c'];
  console.log(lang[1]);


//   Push - this method will add html in the end of the array

  lang.push('html');
  console.log(lang);


    // unshift - add value in the initial state
  lang.unshift('tailwind');
  console.log(lang);

//    pop - remove the value from end
  lang.pop();
  console.log(lang);
//   console.log(lang.pop()); also show the value 

//    shift - remove value from start
lang.shift();
  console.log(lang);


// ********************************



// we can pass objects in array









